# Copy this file to .env and adjust as needed.

# Controls debug output during installation (see Debug::isEnabled()).
# Debug output can include configuration and connection details, so this
# should be "false" in any environment reachable by anyone other than you.
# There is intentionally no way to enable debug output via a URL parameter.
APP_DEBUG=false
